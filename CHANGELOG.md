# Release 分支變更記錄

## [v1.7.4] - 2025-12-21

### 問題修復

#### 徹底修復桌面版生成結果滾動內容底部遮蔽問題

- **調整 max-height 計算值**：將 `contentWrapper` 的 `max-height` 從 `calc(100vh - 260px)` 調整為 `calc(100vh - 220px)`，增加 40px 的可用空間。
- **增加底部 padding**：將 `generatedContent` 的 `padding-bottom` 從 32px 增加到 48px，確保內容有足夠的呼吸空間。
- **移除 JavaScript 高度設定**：簡化 `setupLayoutAlignment` 函數，不再用 JavaScript 設定 `rightColumn.maxHeight`，讓 CSS 完全控制高度。
- **添加 CSS 版本號**：在 HTML 中添加 `?v=1.7.4` 以強制瀏覽器重新載入 CSS。

### 技術改進

- 統一使用 CSS 的響應式設計來控制內容區域高度，避免 JavaScript 和 CSS 的設定衝突。

---

## [v1.7.3] - 2025-12-21

### 問題修復

#### 桌面版滾動內容底部遮蔽問題徹底修復

- **移除固定 max-height 限制**：將 generatedContent 的 max-height 從固定計算值改為 none，讓 flex: 1 自動填滿 contentWrapper 的空間。
- **消除高度差距**：修復前 generatedContent 與 contentWrapper 有 60px 的高度差距，修復後差距為 0px。
- **全裝置支援**：針對所有 5 種響應式斷點統一修改，確保桌面版、平板版和手機版都能正常顯示完整內容。

---

## [v1.7.2] - 2025-12-21

### 🐛 問題修復

#### 滾動內容底部遮蔽問題修復

- **增加內容區域可用空間**：調整所有響應式斷點的 `max-height` 計算值，增加約 40px 的可用空間，確保滾動時底部內容不會被遮蔽。
- **Flexbox 佈局優化**：為 `#contentWrapper` 和 `#generatedContent` 添加 `flex: 1` 和 `min-height: 0`，讓內容區域能夠彈性填滿空間。
- **跨裝置一致性**：針對 5 種螢幕尺寸優化佈局，確保桌面版、平板版和手機版都能正常顯示完整內容。

---

## [v1.7.1] - 2025-12-21

### 🐛 問題修復

#### Scroll Bar 底部遮擋問題修復

- **修復底部內容被截斷問題**：解決了生成結果區塊底部有一塊空白區域遮擋內容的問題，現在內容可以完整滾動到底部。
- **添加底部 padding**：在 `.scrollable-content` 和 `#generatedContent:not(.hidden)` 添加 `padding-bottom: 2rem`，確保內容不被截斷且有足夠的呼吸空間。

#### 響應式設計優化

- **針對 5 種螢幕尺寸優化 max-height 計算**：
  - 大螢幕 (1280px+)：`calc(100vh - 340px)`
  - 標準螢幕 (1024px - 1279px)：`calc(100vh - 360px)`
  - 平板 (768px - 1023px)：`calc(100vh - 320px)`
  - 大手機 (480px - 767px)：`calc(100vh - 280px)`
  - 小手機 (<480px)：`calc(100vh - 260px)`
- **小螢幕字體優化**：在小手機上稍微縮小字體大小至 `0.875rem`，提升可讀性。

---

## [v1.7.0] - 2025-12-21

### ✨ 功能增強、優化與修復

本次更新主要聚焦於 **UI/UX 全面優化**與**跨裝置體驗一致性**，解決了多項佈局、響應式設計和使用者體驗上的問題，大幅提升了系統的視覺美觀性、操作流暢性和資訊層級清晰度。

#### 🎨 核心佈局與對齊優化

- **左右兩側完美對齊**：徹底解決了桌面版左右兩側在各種情況下（包括首次登入、空狀態、內容過多）的高度不對齊問題。現在，無論生成結果為何，左右兩側始終保持視覺上的完美對齊。
- **動態高度調整**：引入了基於 `requestAnimationFrame` 的 JavaScript 動態高度計算，確保在 DOM 完全渲染後才調整佈局，解決了首次載入時的佈局異常問題。
- **Scroll Bar 優化**：當生成結果內容過多時，現在會在區塊內部顯示自定義樣式的滾動條，讓用戶可以滾動查看，而不會破壞整體佈局。透過 `setupLayoutAlignment` 方法動態計算內容區域的可用高度，確保左右兩側始終對齊。
- **Markdown 格式渲染**：針對 API 回傳的 Markdown 格式資料，實作完善的渲染處理，確保分段、斷行、表格、列表等格式清晰呈現，方便使用者閱讀。

#### 🐛 響應式與跨裝置修復

- **Header 固定問題**：修復了在首頁和功能頁滾動時 Header 無法固定的問題。
- **行動裝置與平板佈局**：
  - 修正了 iPhone 15 Pro Max 橫向模式下彈跳視窗的顯示問題。
  - 修正了平板裝置上首頁和功能頁的跑版問題。
  - 增加了手機版 Header 與內容的間距，提升可讀性。

#### 💎 視覺與體驗優化

- **AI 模型下拉選單**：將系統設定中的 AI 模型下拉選單重構為自定義組件，實現了符合全站風格的玻璃效果，並修復了其點擊事件和預設展開的相關問題。
- **彈跳視窗體驗**：修復了彈跳視窗（如系統設定、歷史記錄）開啟時，背景頁面仍然可以滾動的問題，提升了互動的專注度。
- **Footer 設計優化**：修正了 Footer 的背景顏色問題，並優化了其玻璃效果的視覺設計。
- **間距與視覺一致性**：全面審視並優化了頁面各區塊之間的間距，確保整體視覺風格的一致性和呼吸感。

### 🚀 技術實作亮點

- **CSS Grid 與 Flexbox 混合佈局**：靈活運用 `grid`、`flex` 和 `items-stretch` 等現代 CSS 技術，實現了穩固且響應式的佈局結構。
- **JavaScript 渲染時機控制**：透過 `requestAnimationFrame` 和 `window.load` 事件，精準控制佈局腳本的執行時機，解決了因渲染異步導致的佈局錯誤。
- **Marked.js 配置優化**：配置 `marked.js` 的 `breaks` 和 `gfm` 選項，並添加自定義渲染器處理中文公文特殊格式（如【主旨】、【內文】等），提升專業文件的可讀性。

---

*此文件記錄 release 分支的變更，更詳細的開發過程請參考 `CHANGELOG_DEV.md`*
